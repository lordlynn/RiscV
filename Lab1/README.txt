For Windows, had to install new drivers to use printf.

bitstream is uploaded from platform IO. Filepath to bitstream is done in .ini file

excercises 5-10 were not completed as they cover aspects of c programing in general and are not specific to RISCV or Nexys A7.